## What this changes

<!-- One or two sentences. -->

## Why

<!-- What problem it solves. For a board profile: which camera, and what the
     diagnostics showed. -->

## Tested

<!-- Be specific about what ran on real hardware and what did not. A change
     that was never run on a camera is fine -- just say so. -->

- [ ] `dash tests/run-tests.sh` passes
- [ ] `shellcheck -s sh -S warning -e SC2034 ...` is clean
- [ ] Tested on hardware — camera model and what you exercised:

## Notes

<!-- Anything a reviewer should know: assumptions, register values you could
     not verify, follow-up work. -->
